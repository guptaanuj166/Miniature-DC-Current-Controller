For running the code, python is required.

Run the following line on a terminal:

pip install -r requirements.txt

Open serial_conn.py and change the COM port manually.

After this, 

streamlit run serial_conn.py


The current model doesn't account for the simultaneous use of read and write.